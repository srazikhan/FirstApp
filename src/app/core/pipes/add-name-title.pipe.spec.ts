import { AddNameTitlePipe } from './add-name-title.pipe';

describe('AddNameTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AddNameTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
